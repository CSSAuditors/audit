{%- if report -%}
  {%- assign reportSorted = report.list | sort: 'title' -%}
  <div class="table-wrapper">
    <table class="small">
      <thead>
        <tr>
          <th><b>Site</b></th>
          <th><b>Warning type</b></th>
          <th class="tar"><b>Count</b></th>
        </tr>
      </thead>
      <tbody>
        {%- for item in reportSorted -%}
          {%- assign warningsSize = item.warnings | size -%}
          {%- if warningsSize > 0 -%}
            {%- assign warningsSorted = item.warnings | sort: 'key' -%}
            {%- for warning in warningsSorted -%}
              <tr>
                <td>{%- if forloop.index == 1 -%}<b><a href="{{ item.site.url }}">{{ item.site.title }}</a></b>{%- endif -%}</td>
                <td>{{ warning.key | term | upfirst }}</td>
                <td class="tar">{{ warning.value }}</td>
              </tr>
            {%- endfor -%}
          {%- endif -%}
        {%- endfor -%}
      </tbody>
    </table>
  </div>
{%- endif -%}
